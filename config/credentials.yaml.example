# ============================================================
# Radar Test Environment — Credentials Template
# Copy to credentials.yaml and fill in real values.
# credentials.yaml is gitignored. NEVER commit it!
# ============================================================

gitlab:
  url: https://gitlab.radar.iil.intel.com
  project_id: "2"
  access_token: <your-gitlab-token>

jira:
  base_url: https://jira.mobileye.com
  user: <your-jira-user>
  xray_reporting_token: <your-xray-reporting-token>

host:
  sudo_password: <your-sudo-password>

psu:
  ip: 192.168.10.3

